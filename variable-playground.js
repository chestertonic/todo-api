/*var person = {
  name: "Chester",
  age: 26
};

function updatePerson (obj, age) {
  obj.age = age;
}

updatePerson(person, 29);
console.log(person);
*/
var grades = [15, 37];

function addGrade (arr, grade) {
  arr.push(grade);
  debugger;
}

addGrade(grades, 22);

console.log(grades);
